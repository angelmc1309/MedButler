<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.medbutler">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AddMedLayout"></activity>
        <activity android:name=".SettingsActivity" />
        <activity android:name=".SettingsAccountActivity" />
        <activity android:name=".SettingsAboutActivity" />
        <activity android:name=".SettingsAppearanceActivity" />
        <activity android:name=".SettingsHelpSupportActivity" />
        <activity android:name=".SettingsPrivacySecurityActivity" />
        <activity android:name=".SettingsNotificationsActivity" />
        <activity android:name=".MedListActivity" />
        <activity android:name=".DayActivity" />
        <activity android:name=".AdapterSettings" />
        <activity android:name=".Sign_up" />
        <activity
            android:name=".UserProfile"
            android:theme="@style/AppTheme" />
        <activity android:name=".ToolBarTemplate" />
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity
            android:name=".Calendar"
            android:theme="@style/AppTheme" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>